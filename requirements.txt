# ============================================================
# Dependências de runtime — versões fixadas nas testadas localmente.
#
# pyodbc NÃO está aqui: exige unixODBC + Microsoft ODBC Driver 18
# (bibliotecas de sistema) e falha ao compilar no runtime Python do
# Render. Como o app roda sobre mock_data/*.csv, ele não é necessário.
# Para conectar em SQL Server / Fabric: pip install -r requirements-sqlserver.txt
# ============================================================

# --- Servidor WSGI (produção / Render) ---
gunicorn>=23.0.0

# --- Frontend — Dash ---
dash==4.4.1
dash-bootstrap-components==2.0.4
dash-ag-grid==35.3.0
plotly==6.9.0
pandas==3.0.5
numpy==2.5.2
PyYAML==6.0.3

# --- Backend — FastAPI ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
pydantic==2.13.4
pydantic-settings==2.15.0

# --- Autenticação ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
python-multipart==0.0.32

# --- IA (Captura Assistida) ---
anthropic==1.2.0

# --- Utilitários ---
python-dotenv==1.2.3
